name: flutter_quick_learn
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  http: ^0.13.3
  flutter_bloc: ^7.2.0
  assets_audio_player: ^3.0.3+6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
